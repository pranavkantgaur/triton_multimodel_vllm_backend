# Runtime dependencies for the multi-model LLM manager service.
# Pin minor versions for reproducibility; update as needed.

fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
httpx>=0.27.0,<1.0.0
pyyaml>=6.0.1,<8.0.0
pydantic>=2.6.0,<3.0.0

# Optional: Triton gRPC client (used by the example client)
tritonclient[grpc]>=2.43.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
